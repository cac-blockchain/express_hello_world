apiVersion: v1
kind: Pod
metadata:
  name: hello-world
  labels:
    app: hello-world
spec:
  containers:
  - name: hello-world
    image: local/hello-world:v1
    imagePullPolicy: Never
    ports:
      - containerPort: 3000
